# Base Parameters #

This is a package containing a C++ base parameter class. The package provides base functionalities such as reading and writing parameters from and to yaml files, printing the parameters to be used, as well as logging possible errors.
